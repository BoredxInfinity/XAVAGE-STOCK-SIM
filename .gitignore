node_modules/
.next/
out/
build/
# Every .env variant, then re-admit the templates. The old three patterns
# (.env, .env.local, .env*.local) left root .env.production, .env.development
# and .env.staging tracked -- verified with `git check-ignore` -- and
# worker/.env.production already exists, so the root twin was a plausible next
# file to create and commit.
.env
.env.*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# local Supabase CLI state
supabase/.temp/
supabase/.branches/
worker/.env*
!worker/.env.example

# worker deploy bundle (built by worker/build-bundle.sh)
xavage-worker-bundle.tar.gz
worker/libs/

# local load-testing harness (loadtest/) — throwaway, never deployed
loadtest/
